apiVersion: v1
kind: Service
metadata:
  name: helloworld-service
  labels:
    app.name: helloworld
  spec:
    type: ClusterIP
    ports:
      - port: 8000
        targetPort: 8000
        protocol: TCP
        name: helloworld-port
    selector:
      app.name: helloworld